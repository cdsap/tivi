version: 2.1
executor: machine
jobs:
  build:
    docker:
      image: circleci/android:api-28

    working_directory: ~/repo


    environment:
      JVM_OPTS: -Xmx5G
      TERM: dumb

    steps:
      - checkout
      - run:
          name: build
          command: ./gradlew app:assembleDebug --scan --stacktrace --info


workflows:
  version: 2.1
  build-snap:
    jobs:
      - build
